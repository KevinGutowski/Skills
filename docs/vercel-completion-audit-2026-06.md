# Vercel Overhaul Completion Audit

Date: 2026-06-26
Fresh judge completed: 2026-06-29
Objective: apply the Vercel product-design-agent pattern to the local skill library by planning, revising/re-referencing skills, and testing the result.

## Requirements And Evidence

| Requirement | Evidence | Status |
| --- | --- | --- |
| Review the Vercel article and derive an operating pattern. | `docs/vercel-product-design-overhaul-2026-06.md` records the article source and translates it into skill body, references, exemplars, coverage gaps, deterministic checks, and human review. | Proven. |
| Produce a plan before implementation. | `docs/vercel-product-design-overhaul-2026-06.md` contains the staged plan, pilot cluster, rewrite standard, pass template, and decision form. | Proven. |
| Audit every skill rather than only editing favorites. | `docs/vercel-skill-audit-2026-06.md` contains 39 rows; `scripts/check_vercel_overhaul.py` verifies 39 skills and 39 audit rows with no missing/extra/duplicate entries. | Proven. |
| Tweak skill bodies where the Vercel pattern changes routing or behavior. | Modified `SKILL.md` files add source/gap pointers, Vercel-pattern boundaries, and route clarifications across P0/P1/P2 skills. P3 skills were audited and left lean by explicit matrix decision. | Proven for audited scope. |
| Re-reference skills against source support and park weak candidates. | Every skill with `references/` now has top-level `sources.md` and `coverage-gaps.md`; `scripts/check_vercel_overhaul.py` verifies both files exist and are linked from P0/P1/P2 `SKILL.md` files. | Proven. |
| Add deterministic testing/governance inspired by the article. | `scripts/check_vercel_overhaul.py`, `scripts/check_vercel_routing_probes.py`, `scripts/build_vercel_probe_packet.py`, `docs/vercel-routing-probes-2026-06.md`, and `docs/vercel-routing-probe-results-2026-06.md`. The probe checker also sync-checks `~/.claude/skill-evals/routing-eval.md` probes 115-129 when present. | Proven. |
| Validate the whole library. | Latest local checks pass: `validate_all.py`, `check_xrefs.py`, `check_vercel_overhaul.py`, `check_vercel_routing_probes.py`, and `git diff --check`. | Proven. |
| Test routing with a fresh independent judge. | User authorized the fresh judge on 2026-06-29. Kepler (`019f15ca-9c65-7251-b637-4c96f47c92b4`) used only the generated descriptions/probes packet with `fork_context=false` and passed 15/15 probes; details are recorded in `docs/vercel-routing-probe-results-2026-06.md`. | Proven. |
| Smoke-test every skill. | `docs/per-skill-smoke-tests-2026-06.md` defines one representative routing prompt for each of 39 local skills. `scripts/check_skill_smoke_tests.py` verifies full fixture coverage, and Wegener (`019f15da-c92e-79f1-8f52-6f3e1db99bdc`) used only the generated packet with `fork_context=false` and matched 39/39 first-skill choices. | Proven. |

## Current Completion Decision

The implementation, source/gap scaffolding, deterministic checks, local validation, fresh independent routing judge, and per-skill smoke pass are complete. All stated requirements are proven by current artifacts and validation, so the goal can be marked complete.

## Fresh-Judge Prompt

Use this after explicit authorization:

```text
Using only the descriptions and probes in the packet generated by:

python3 scripts/build_vercel_probe_packet.py

For each V1-V15 probe, choose the first skill you would load. If a second skill is required, name it separately. Do not inspect the skill bodies and do not execute the tasks. Return a table with probe id, chosen first skill, optional second skill, pass/fail against the packet expectation, and one sentence of rationale.
```
