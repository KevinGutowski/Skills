# AI UI Direction

*Scope: Direct and fix AI-generated UI — diagnose layout bugs in primitive vocabulary (fixed/fill/hug, padding vs gap, nesting), prompt surgically, choose prompt-vs-edit, run the independent-judge loop. Use when AI UI output looks wrong. Generation itself → frontend-design; agent mechanics → agentic-coding. Triggers: AI generated UI, fix this layout, vibe coded UI, AI slop, v0 output.*

**Sources:** [ai-ui-direction/sources.md](ai-ui-direction/sources.md) — MDS's layout-primitive essays + Cursor/Notion practitioners + 11 Dive Club interviews + external de-slop skills (Impeccable, interface-design, baseline-ui, Addy Osmani).

The task shape: an AI produced UI and you have to make it *right* — which is a directing job, not a generating job. The thesis (MDS): **layout-primitive literacy is the prompting skill.** If you can't name what's wrong in the layout system's own vocabulary, "sometimes not having the knowledge makes it where you don't even know what questions to ask" — you prompt into the ether and iterate blindly. The master frame (Brian Lovin, Dive Club podcast, dvEwb1Ajkwo): "we just need to make our intent legible to AI" — primitive vocabulary, named layers, reference URLs, and codified taste are all intent-legibility moves. Independent corroboration from the critique tradition (Katarina Batina, Dive Club podcast, 0YjO7wShTkQ): "our material language for building is now becoming words. We now have to give critique to our agents… The higher fidelity that designers can have in the language they use to make their work better, the better off they're going to be in an agentic era" — vague language ("this doesn't feel right") means "you're probably not going to one-shot something." Two lineages, same skill.

## Speak in primitives

- **The three sizing behaviors** — fixed width, fill container, hug contents — are the diagnostic vocabulary (identical in Figma auto layout and CSS): "like a jacket hugging a body… if your jacket was set to fixed and your shirt was set to hug." Fill is only meaningful inside a nested container; hug suits vertical content (it can scroll), fill suits horizontal. Width and height are set *independently* — half of "broken" AI layouts are one axis on the wrong behavior.
- **Decompose before you prompt.** Real dashboards are "about three levels" of nested containers. Name the level the bug lives at (component-internal padding vs parent gap vs grid track) and address the instruction there — not "make it look better."
- **Derive constraints empirically, late.** Resize until it breaks, note the number ("minimum width on these of 184"), and only add min/max constraints "as you need them" — premature constraints in root components are how systems rot.
- **Know the framework axioms, skip the depths**: "the base pixels in Tailwind are 4 pixels. I don't need to know why." The floor for directing code is primitive fluency, not CSS mastery.

## Recognize the failure modes

- **Class-name litter**: when you can't name what you want, the model starts "littering the code with a bunch of class names" and "could just inject a bunch of random code that might make you struggle with your layout" — wrapper divs breeding wrappers. The fix is a more primitive-precise instruction, not another vague retry.
- **Token substitution**: models silently swap your values for framework defaults (Jin Park's pipeline: the AI used Tailwind's default colors — "pull the hex values and apply them as custom arbitrary values"). Diff exact colors, radii, spacing against the source design.
- **Detail loss in translation**: hand an agent a Figma mock and you lose the invisible specifics — "you want this easing curve to be exactly this value with like 0.2 like seconds" (Ryo). Carry motion and micro-detail as explicit specs, or do them yourself and hand the result over: "you do it. And then you can give it over and say, 'This is what I'm talking about'" (MDS).
- **The named-failure catalog** (Karl Koch, Dive Club podcast, 7_VEb9iDW2c) — keep a list of recurring AI bugs *with names*, so you can catch them on sight:
  - *useEffect-wraps-everything*: "The AI will go on this huge rampage of trying to like solve the Flash, but not understand that it created the Flash." Remedy: keep React's "You Might Not Need an Effect" as a repo reference doc and point the agent at it — "refer to this document" — and it finds its own unnecessary effects.
  - *Index-as-key* on list items — fine-looking until reorder/insert breaks identity.
  - *Spring-everything*: "it springs everything. Everything's a spring." Steer with named motion vocabulary instead — say "clip path," "morph" — don't accept the spring default.
  - Triage in DevTools before re-prompting: "the best place to debug a problem is is just in the browser." And accept Karpathy-style two-and-done: "it took you to know that it didn't look right to prompt it to then fix it" — the second prompt is the skill, not a failure.
- **Model-specific visual-defect catalogs** (Paul Bakaus, Impeccable, github.com/pbakaus/impeccable): the named-failure catalog above names behavioral bugs; keep a parallel per-model list of *visual* tells, dated, because tells drift with training data. Impeccable's "Codex-specific defects" (2026) are the template: over-rounding (`border-radius: 32px+` on cards), "hand-drawn / sketchy SVG" fallback illustrations, `repeating-linear-gradient` stripe backgrounds, decorative grid overlays. When a model updates, re-verify the list before enforcing it. (The corresponding generation-side numeric tells live in `frontend-design`.)
- **Deslop in two passes — squint, then scan** (Damilola Jay, interface-design's design-deslop reference): "Slop is compositional. So you can't catch it by reading CSS line by line; you have to look at the rendered thing first, then go fix the lines." Pass 1 squints the RENDERED output for the ranked compositional tells: no focal point, flat hierarchy, monotone layout, timid color, "borders doing the work of space." Pass 2 scans the diff for line-level tells: generic token names, `transition: all`, missing hover/focus/active/disabled states ("the fastest tell of unfinished UI"), off-grid spacing values, hand-rolled controls beside existing accessible primitives.
- **Know what is NOT slop** (same source — "this filter is half the skill"): a motivated bold choice, an intentional deviation with a reason, choices the project's design memory already ratifies, and anything outside the diff are off-limits — "generated UI looks generated because it defaulted; it does *not* become good by defaulting harder." Unsure whether it's a tell or a decision? Treat it as a decision; note it, don't churn it.
- **Diagnose tells by their causes** (Addy Osmani, agent-skills): each AI default has a reason, and the reason points at the fix — purple/indigo everywhere is convergence on visually "safe" palettes (fix: the project's actual palette); lorem-ipsum copy "hides layout problems that real content reveals" (fix: realistic content); uniformly generous padding "destroys visual hierarchy"; uniform card grids ignore "information priority and scanning patterns." Ask what the default was optimizing for, then supply the specific thing it dodged.
- **The baseline-pass school** (Julien Thibeaut / ibelick, ui-skills baseline-ui, MIT): a compact, checkable de-slop baseline run as one repair pass. Its most durable portable rules: animate compositor props only, interaction feedback ≤200ms; `h-dvh` never `h-screen`, respect `safe-area-inset` on fixed elements; errors adjacent to the action; never block paste; empty states get one clear next action; one accent per view; no gradients/glow unless asked; a fixed z-index scale. (Its `text-balance`/`text-pretty` and `tabular-nums` rules already live in `design-craft` / `web-design`.) Caveat: its stack rules (Tailwind defaults, motion/react, Base UI) are stack-scoped, not universal.
- **Wrong reference point** (Kyle Xantos, Dive Club podcast, oYc_dF95VAE): when output ignores instructions, diagnose the model's anchor, not its effort — "If you thought you did what I asked you to, your reference point is wrong." The repair script displaces the old state explicitly ("completely change what you have for the matchups tab, *not what you had before*") and hands it the correct reference (he screenshots the current state and pastes it in).
- **Probe the vision model like a printer calibration sheet** (Louis Healey, Atlassian, Dive Club podcast, CqMZTg7L-wE): "I want to calibrate your computer vision model… add the image in and then create bounding boxes" — make it name what it sees before trusting screenshot-driven prompts. Finding: "the more complex the screenshot, the less it picked up — and actually it truncated and missed a bunch of elements" (low side-nav items; small icon tiles whose color "it didn't pick up at all" → hallucination). The user-facing rule: "upload a smaller screenshot or a zoomed-in screenshot… and update it piecemeal."
- **Reference-corpus overfit** (Jaytel Taste): if an auditor or repair prompt uses a taste skill, check whether the rules came from the current corpus or from a famous example skill. A neutral dashboard skill should not ban serif/editorial/fashion treatments for a poster corpus; a luxury corpus should not import beige-commerce defaults unless the source images actually show them. Ask: "Which visible evidence supports this rule?"
- **Jelly-bean-factory under-specification** (Andy Madrick, Dive Club podcast, IfPK0LwbX_0): vague style prompts yield "a million gradients and like a million tags… a jelly bean factory" — trim down, or pre-constrain with your design system (remixing legacy components through it: "we had all the pieces here").
- The element-by-element diff checklist for mock-to-code output (type weights/sizes, exact colors, corner radii, spacing, completeness, gradient scale) lives in `design-prototyping`'s agents tier.

## Direct the loop

1. **Substrate first** (Ryo): generate onto a constrained base — shadcn/Radix-grade primitives and "a really robust like foundational set of tokens and components" — because AI "is really good at composing patterns that exist." Then theme by hand. (Full de-slop rules → `frontend-design`.)
2. **Prompt surgically, in primitive vocabulary**, scoped to the right container level. Batch-generate options and cull ("five vastly different hover effects" → keep one, ask for five more in its direction).
   - **For polish, prompt buildable values, not adjectives** (Kevin Kold, X Article 2064972712729292800): "smooth/premium/polished" is not an instruction. Give the actual motion curve, duration, offset, blur, shadow stack, radius token, state list, and allowed variables. Start with the token block and say "use only these tokens, no one-off values." Then isolate each polish pass: "only tune the shadow stack," "only tune the entrance," "only tune pressed state." One variable at a time prevents the agent from compensating in unrelated places.
   - **Hand Figma properties over explicitly.** When using Figma/MCP context, do not assume the bridge preserves every relevant style. Point at the current selection and list the properties the agent must read: padding, gaps, token names, colors, radii, type sizes/weights, shadows, motion notes, and known variants. Treat the file as a hypothesis for the build, not the final state system.
3. **Prompt vs edit**: don't prompt "change P8 to P4" when you can edit the value directly (MDS's named anti-pattern) — prompting is for structure and behavior; direct edits are for values you can already see. Lovin's version: "Why are you spending money to nudge something in code? Just like go change the class" — with the **batch exception**: "I'm okay saying nudge this four pixels to the left if I can bundle that with like six other prompts" (one agent run amortizes many nudges; lone nudges get hand-edited). Third path (Kyle Santos, Dive Club podcast, HcLz3ikw-n0): a visual editor where you tweak values directly then "hit apply" to write them back through the agent — "keeping you out of the prompt loop." The end-state role: "AI pilots" — "there's still going to be the need for someone to understand what they're doing and what makes it good."
   - **Rip techniques from reference URLs** (Santos): "I dropped in this URL and I said, 'This technique is awesome. I want it applied to our components. Go do that'" — then extrapolate the theory, not the snippet: "take the same theory but make it stronger. Maybe do like five layers."
   - **Codify taste as an auditor** (Santos): curate ~15–20 CodePens/posts/tweets from designers you admire into a skill that runs as a "visual design interaction design auditor" subagent. Iterate by re-synthesizing, not stacking: zoom "back to the source material and think about what the general philosophy becomes" when adding a perspective. Caution: "don't don't create one skill that is every designer you've ever thought was cool." When the source is a visual reference corpus rather than posts/code examples, use `creating-skills` (converting-visual-references-to-skills) to extract the taste rules; this reference owns applying those rules as an auditor or repair loop.
   - **Style-transfer merge** (Madrick): when a designer build and an engineer build diverge, don't reconcile by hand — "Can you keep all the functionality of the engineer's version and give it all the styling that I have here." For motion, prompt with a labeled keyframe sketch — "a picture's literally worth a thousand words" — labeling each state in the drawing.
4. **Prompt the symptom outside your wheelhouse** (Ryan Moshi, Dive Club podcast, onQY0PrUulw): when you can't name the right mechanism, prescribing one is risky — telling it "just use transform scale or just like scale up the canvas size, that might have been bad direction. Instead, just telling it like here's what's happening. It's blurry. Can you make it sharp?" Two failed tries → "dig in and try to learn it myself." (Inside your wheelhouse, primitive vocabulary still wins — this is the fallback, not the default.)
5. **Sketch beats words for spatial intent** (Ron Goldin, Dive Club podcast, UkQpgslyR3A): "when something is off in Claude, I could back and forth with it in words for like 10 minutes or I could go to Figma and sketch it out in 5 minutes" — dual-task between the agent building and a sketch surface. Same family as Madrick's labeled keyframe sketch above. And have the agent reflect rambles back (Marvin Schwaibold, Dive Club podcast, KpJs7mZYErg): "ask me clarifying questions if you're unsure of my prompt cuz I'll ramble or send a voice note" — then it spits "basically my idea back to me" for confirmation before building.
6. **Evaluate agents by how they drive** (Tommy Geoco, Dive Club podcast, OYNoy468kS8): don't judge a model/tool on one-shot output — "you have to drive them for a little while. When you drive it with intent and you're just like… that's wrong fix, that's wrong fix, and you do that a few iterations, I think then you have a good feel." (Turning those corrections into standing rules → [agentic-coding.md](agentic-coding.md) / `creating-skills`.)
7. **Build once, then discover missing states by touching it.** A static mock rarely names every state. After implementation, operate the component and add the states the live thing reveals: idle, hover, pressed, loading/working, disabled, success/error, empty/overflow, snap-caught, drag-release, and reduced-motion. If a number hard-cuts, make it roll or cross-fade; if a task is working, prefer a calm state treatment over a default spinner when the wait is short and controlled. The user feels the state system, not the screenshot.
8. **Judge in a fresh context**: have an independent judge with its own context window evaluate the output against explicit criteria (the pattern, loadout, and rationale live in `design-prototyping`'s agents tier #4; criteria sources: `design-principles`, `design-craft`).
9. **Fundamentals gate the polish stage** (MDS's skateboard maxim): "Have fun dropping into the bowl" — speed is real, but output you can't diagnose is output you can't finish.

## Checklist

- [ ] Complaint translated into primitive vocabulary (which container, which axis, which behavior) before prompting?
- [ ] Generation running on a constrained substrate (real tokens + primitive components), not from scratch?
- [ ] Output diffed against source for token substitution and detail loss (colors, radii, spacing, motion values)?
- [ ] Polish prompts use exact values/tokens and one isolated concern, not adjectives like "premium" or "smooth"?
- [ ] Known Figma properties and component states were listed explicitly, and live use surfaced any missing states?
- [ ] Taste/auditor rules traced to visible source evidence, not borrowed from a different reference corpus?
- [ ] Deslop run in two passes — squint the render first, then scan the diff — with motivated/ratified choices left alone?
- [ ] Value tweaks edited directly; prompts reserved for structure/behavior?
- [ ] Independent fresh-context judge run before calling it done?

## Relationship to other skills

- **`frontend-design`** — generates the UI well (de-slop substrate, distinctive aesthetics); this skill takes over when output needs direction and repair.
- **`design-prototyping`** — the agents tier owns the broader prototyping workflow, the mock-diff checklist, and the judge pattern; this skill is its layout-diagnosis sharp end.
- **[agentic-coding.md](agentic-coding.md)** — agent workflow mechanics (plan mode, prompt protocol, topology); this skill is UI-specific direction.
- **`creating-skills` (converting-visual-references-to-skills)** — turns a curated image corpus into reusable visual-taste criteria; this skill uses those criteria to steer, audit, and repair generated UI.
- **`design-systems`** — the bricks/DS-as-AI-context doctrine that makes step 1 possible.
- **`design-craft`** (incl. its `emil-kowalski` reference) — the polish criteria the judge enforces.

> **Staleness note (Kevin's rule):** Tailwind/Figma specifics (4px base, auto-layout terms) are 2025–26-era tooling; the primitive-literacy thesis, failure-mode taxonomy, and direct-the-loop discipline are the durable layer.
